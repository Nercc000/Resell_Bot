# Caddyfile for api.resellerbot.de (Using BuyPass CA)

# Global options
{
    email digga9286@gmail.com
    # Switch to BuyPass Go SSL (Free, valid for 180 days)
    acme_ca https://api.buypass.com/acme/directory
}

# Site block for the IP address
api.resellerbot.de {
    reverse_proxy ps5-bot-backend:8000
}
