# Caddyfile for nip.io fallback (Bypasses rate limit)

# Global options
{
    email digga9286@gmail.com
}

# Site block for the IP-based domain
# Site block for the IP-based domain and real domains
45.147.7.54.nip.io, www.resellerbot.de, resellerbot.de {
    reverse_proxy ps5-bot-backend:8000
}
